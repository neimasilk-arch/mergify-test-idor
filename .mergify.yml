queue_rules:
  - name: default
    merge_conditions:
      - check-success=ci

pull_request_rules:
  - name: math-only
    conditions:
      - label=ssti-test
    actions:
      comment:
        message: "RESULT={{7*7}} TITLE={{title}}"
