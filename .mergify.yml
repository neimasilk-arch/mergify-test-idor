pull_request_rules:
  - name: pprint-test
    conditions:
      - label=ssti-test
    actions:
      comment:
        message: |
          CONFIRM={{7*7}}
          REVERSE={{(lipsum|string)|reverse}}
          LOOP_DEPTH={% for i in range(2) %}D{{loop.depth}},{% endfor %}
