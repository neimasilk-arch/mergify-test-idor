queue_rules:
  - name: default
    merge_conditions:
      - check-success=mergify-ci

pull_request_rules:
  - name: queue approved PRs
    conditions:
      - approved-reviews-by>=1
    actions:
      queue:
        name: default
